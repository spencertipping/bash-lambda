#!/bin/bash
(cat src/header
 for file in heap ref gc fn fp multi \
             pipelock semaphore atom \
             list \
             future remote wait parallel init; do
  cat src/$file | grep -v '^#!'
  echo; done) > bash-lambda

chmod +x bash-lambda
